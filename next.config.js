/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // App Router is enabled by default in current Next.js versions.
  // Removed `experimental.appDir` to avoid invalid-next.config warnings.
}
module.exports = nextConfig
